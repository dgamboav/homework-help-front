import { types } from "../types/types";

const initialState = {
    lodading: false
}

export const uiReducer = ( state = initialState, action) {
    switch (action.type) {
        case types.uiSetLoader:
            
            break;
    
        default:
            break;
    }
}